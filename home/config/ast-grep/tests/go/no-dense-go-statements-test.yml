id: no-dense-go-statements
valid:
  - processor := service.NewProcessor(store, options)
  - 'return executeTask(ctx, runtime.worker, options, runtime.credentials, primaryStore, log.Named("worker"))'
  - |-
      processor := service.NewProcessor(
        store,
        cache,
        client,
        processorOptions,
        logger.Named("processor"),
      )
invalid:
  - processor := service.NewProcessor(store, cache, client, processorOptions, logger.Named("processor-with-an-intentionally-long-name"))
  - 'return executeTask(requestContext, runtime.worker, options, runtime.credentials, primaryStore, disabledInputs, metadataParser, log.Named("worker"))'
