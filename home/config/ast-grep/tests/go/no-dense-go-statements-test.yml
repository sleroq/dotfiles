id: no-dense-go-statements
valid:
  - automatic := aipipeline.NewAutomaticService(db, config)
  - 'return runBotClient(groupCtx, telegram.botRuntime, config, telegram.account, investNewsWatcherDB, lg.Named("bot"))'
  - |-
      automatic := aipipeline.NewAutomaticService(
        db,
        sheetStore,
        ai,
        automaticConfig,
        logger.Named("automatic"),
      )
invalid:
  - automatic := aipipeline.NewAutomaticService(db, sheetStore, ai, automaticConfig, logger.Named("automatic-with-an-excessively-long-name"))
  - 'return runUserClient(groupCtx, telegram.userRuntime, config, telegram.account, investNewsWatcherDB, imageProcessingDisabledSources, authorExtractor, lg.Named("user"))'
