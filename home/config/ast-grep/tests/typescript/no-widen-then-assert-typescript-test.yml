id: no-widen-then-assert-typescript
valid:
  - |-
      const parsed = value as Payload;
  - |-
      const broad = value as unknown;
invalid:
  - |-
      const parsed = (value as unknown) as Payload;
  - |-
      const parsed = <Payload><unknown>value;
