---
name: rust
description: Use for any Rust implementation, review, refactor, debugging, or testing work.
---

After changing Rust code, run `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `cargo machete` from the workspace root. Report any failures honestly.
